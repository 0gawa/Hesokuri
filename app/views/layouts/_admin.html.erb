<header class="mb-5 bg-dark">
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <h1 class="p-1 text-light">管理者</h1>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse flex-row-reverse" id="navbarNavDropdown">
                <ul class="navbar-nav ml-auto">
                    <% if admin_signed_in? %>
                        <li class="nav-item header_btn me-4">
                            <%= link_to "トップ", dashboards_path, class: 'nav-link text-light' %>
                        </li>
                        <li class="nav-item header_btn me-4">
                            <%= link_to "収入の入力", new_income_path, class: 'nav-link text-light' %>
                        </li>
                        <li class="nav-item header_btn me-4">
                            <%= link_to "支出の入力", new_spend_path, class: 'nav-link text-light' %>
                        </li>
                        <li class="nav-item header_btn me-4">
                            <%= link_to "ユーザー情報", edit_user_path, class: 'nav-link text-light' %>
                        </li>
                        <li class="nav-item header_btn me-4">
                            <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: 'nav-link text-light' %>
                        </li>
                    <% else %>
                        <li class="nav-item header_btn me-4">
                            <%= link_to "トップ", admin_root_path, class: 'nav-link text-light' %>
                        </li>
                        <li class="nav-item header_btn me-4">
                            <%= link_to "ログイン", new_admin_session_path, class: 'nav-link text-light' %>
                        </li>
                    <% end %>
                </ul>
            </div>
        </div>
    </nav>
</header>